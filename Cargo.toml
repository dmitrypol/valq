[package]
name = "valq"
version = "0.1.0"
edition = "2024"

[dependencies]
valkey-module = { version = "0.1.10" }
#valkey-module-macros = { version = "0.1.10" }

[lib]
crate-type = ["cdylib"]

[lints.clippy]
unwrap_used = "forbid"
